{
  "name": "mcp-demo-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required for this Express.js server'",
    "dev": "node --watch server.js",
    "start:oauth": "node server-oauth.js",
    "dev:oauth": "node --watch server-oauth.js"
  },
  "engines": {
    "node": "20.16.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "@supabase/supabase-js": "^2.39.1"
  }
}
